name: publish-mcp

# 0.15.1: list vibatchium on the official MCP registry
# (registry.modelcontextprotocol.io) under the io.github.trueoriginlabs/*
# namespace. We authenticate with GitHub Actions OIDC — because the workflow
# runs in a repo owned by `trueoriginlabs`, the registry grants that org
# namespace automatically. This deliberately sidesteps the interactive
# `mcp-publisher login github` OAuth flow, which the org's third-party OAuth
# App access policy blocks (the registry app is unapproved, so GitHub hides
# org membership from it even for public members).
#
# Triggers:
#   - workflow_run on `publish` -> auto-list every tagged release once the
#                            PyPI + GitHub Release pipeline has SUCCEEDED, so
#                            the registry never points at a version that
#                            failed to ship.
#   - workflow_dispatch    -> run standalone against the current server.json
#                            (e.g. to backfill an already-shipped version).
#
# WHY NOT `release: published`: that was the original trigger and it NEVER
# FIRED — 0.15.1 through 0.18.9 all had to be dispatched by hand. publish.yml
# creates the release with `gh release create` using the default GITHUB_TOKEN,
# and GitHub deliberately suppresses workflow triggers from events raised by
# that token (the recursion guard). `workflow_run` is not subject to it.
#
# NOTE: server.json's `version` is the source of truth for what gets listed;
# bump it alongside vibatchium/__init__.py when cutting a release.
# workflow_run checks out the default branch, which is where that bump lands
# before the tag is pushed — so master is the right ref to read it from.

on:
  workflow_run:
    workflows: ["publish"]
    types: [completed]
  workflow_dispatch:

permissions:
  id-token: write   # required for OIDC
  contents: read

jobs:
  registry:
    # Never list a version whose PyPI publish failed.
    if: >-
      github.event_name == 'workflow_dispatch' ||
      github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Install mcp-publisher
        run: |
          curl -sSL \
            "https://github.com/modelcontextprotocol/registry/releases/download/v1.8.0/mcp-publisher_linux_amd64.tar.gz" \
            | tar -xz mcp-publisher
          ./mcp-publisher --help >/dev/null

      - name: Validate server.json
        run: ./mcp-publisher validate server.json

      - name: Authenticate via GitHub OIDC
        run: ./mcp-publisher login github-oidc

      - name: Publish to the MCP registry
        run: ./mcp-publisher publish server.json
